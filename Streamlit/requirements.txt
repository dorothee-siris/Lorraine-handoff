# PINNED = the verified stack. Streamlit Community Cloud re-resolves this file on every
# deploy, so version floors ship an environment nobody ever tested (BUILD_PLAN §7, D61):
# the unpinned floors resolved to plotly 6.9 / pandas 3.0 / numpy 2.4, and the old
# `wordcloud==1.9.3` has no CPython 3.13 wheel at all. Every pin below is verified to have
# wheels for CPython 3.11, 3.12 and 3.13 (the repo does not control the cloud's Python),
# and `python tests/ui/smoke.py` + `pytest tests/test_app_numbers.py` are green against
# exactly this set. Bump deliberately, then re-run both.
streamlit==1.61.1
pandas==2.3.3
numpy==2.4.6
plotly==5.24.1        # last 5.x. All 13 charts are pixel-identical on 6.9.0 since the
                      # offsetgroup fix in pages/1 - the pin is determinism, not a crutch.
pyarrow==24.0.0
matplotlib==3.11.1
wordcloud==1.9.6      # 1.9.3 (the previous pin) has no cp313 wheel
psutil==7.2.2
pyyaml==6.0.3         # the app reads the pipeline's config.yaml (app.sdg_variant, D51)
openpyxl==3.1.5       # W5 xlsx export layer (lib/exports.py); latest, pure-Python
                      # (universal wheel -- no cp311/312/313 compile step to verify)
# Dropped: `unidecode` and `streamlit-plotly-events` - imported by no module in Streamlit/
# (grep-verified). streamlit-plotly-events is unmaintained and constrains streamlit.
